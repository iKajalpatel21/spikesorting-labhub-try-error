When you run these two :

1. python manage.py makemigrations
2. python manage.py migrate

You should see this output :

Migrations for 'spikejobs':
  spikejobs/migrations/0001_initial.py
    - Create model Experiment
...
Applying spikejobs.0001_initial... OK

But instead I was getting "No changes detected"
Reason : 

What really happened:
When you saw this error:

django.db.utils.OperationalError: no such table: spikejobs_experiment
…it meant Django was trying to access a table that hadn’t been created yet — likely due to missing migrations or skipped migration steps.

Your fix:

rm -rf spikejobs/migrations/
rm db.sqlite3
python manage.py makemigrations spikejobs
python manage.py migrate

Removed any old or broken migration files 🧹

Reset the database 🧼

Recreated a clean schema based on your current models 🧱

--------------------- Execute the sqlite3 ----------------------
-> sqlite3 db.sqlite3
-> .tables
-> SELECT * FROM spikejobs_experiment;

--------------------- To run the server ----------------------

python manage.py runserver
When you use python manage.py runserver, Django uses HTTP by default.

--------Push everything to git----------
git status
git add .
git commit -m "Implement worker fetch logic and result updates + API cleanup"
git push origin main



------Server start ----
uvicorn labhub.asgi:application \
       --host 127.0.0.1 --port 8000 \
       --ssl-keyfile key.pem --ssl-certfile cert.pem


---Json url----
https://127.0.0.1:8000/api/experiments/
https://127.0.0.1:8000/submit-json/